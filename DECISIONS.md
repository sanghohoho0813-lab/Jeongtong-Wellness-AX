# DECISIONS — 왜 이렇게 만들었나 (WHY · WHY NOT · REVISIT WHEN)

> Unified v3.0 §U-6 「프로젝트 메모리」. 한 번 정한 것을 다시 묻지 않기 위해 적는다.
> 뒤집을 조건(REVISIT WHEN)이 오기 전까지는 이 결정을 따른다.

| # | 결정 | WHY | WHY NOT (버린 대안) | REVISIT WHEN |
|---|---|---|---|---|
| D-01 | **규칙 엔진(L1/L2)으로 우선순위를 뽑고, 머신러닝은 쓰지 않는다** (`lib/scoring/priority.ts`, LOCKED) | 실명부 12명 · 방문 기록 0건. 학습할 자료가 없다. 규칙은 직원이 이유를 읽을 수 있다 (「이용권 2회 남음 · 21일 미방문」). | ML 이탈 예측 — 자료가 없으니 확률은 지어낸 숫자가 된다. v3.0 「허위 AI 확률」 금지. | 실운영 방문 기록 60건 · 재등록/이탈 결과 라벨이 쌓인 뒤 (RECOMMENDATIONS R-02). |
| D-02 | **기기 저장(localStorage)이 기본, 서버(Supabase)는 설정으로 켠다** | 대표 부부(60대)가 계정 · 키 없이 오늘 바로 쓸 수 있어야 한다. 서버는 「직원 연결」 이 되면 자동으로 이중 저장. | 서버 필수 — 첫날부터 로그인 장벽. 오프라인 매장 Wi-Fi 끊김에 기록이 막힌다. | 2호점이 생겨 같은 명부를 두 기기에서 동시에 봐야 할 때 → PRODUCTION 단계로 승격. |
| D-03 | **단계 표시(DEMO / PILOT / PRODUCTION)를 화면 상단에 항상 둔다** (`lib/stage.ts`) | 심사자 · 직원이 지금 보는 숫자가 견본인지 실제인지 헷갈리면 안 된다 (v3.0 §15). | 설정 안에만 적기 — 성과 화면에서 안 보인다. | 없음 (영구). |
| D-04 | **개선율은 기준선 · 현재값이 둘 다 있고, DEMO 가 아닐 때만 적는다** (`KpiContract.tsx`) | v3.0 §4.2 TARGET: DO NOT INVENT. 견본 자료로 「+37% 개선」 을 찍으면 P0 FAIL. | 견본에서도 개선율 표시 — 시연은 화려하지만 허위 성과. | 없음 (영구). |
| D-05 | **도입 전 기준선은 사람이 설정에 적고, 출처를 「수기 기준선」 으로 남긴다** | 수기 운영 시절 자료는 시스템에 없다. 기억하는 만큼만 적고 출처를 숨기지 않는 것이 가장 정직하다. | 기준선을 비워 두고 개선을 영영 못 재기 / 견본 값으로 채우기. | 실운영 4주 뒤 같은 단위로 다시 재서 나란히 놓을 때. |
| D-06 | **기존 6개 테마의 hex 값은 손대지 않고 3개를 덧붙였다** (07 네이비 골드 · 08 에메랄드 골드 · 09 스틸 플래티넘) | 이미 고른 테마가 갑자기 다른 색이 되면 「기능이 바뀌었다」 로 느낀다. v3.0 Q-1 9종 요구는 추가로 충족. | 9종을 v3.0 예시 hex 로 전부 교체 — 설정 회귀 · 대비 재검증 비용. | 대표님이 기존 색 중 하나를 바꿔 달라고 할 때 (그때도 그 하나만). |
| D-07 | **견본 명부는 매장 실제 명부 12명의 이름을 그대로, 전화번호는 비운다** (`lib/data/seed.ts`) | 사용자 지시. 시연에서 낯익은 이름이 나와야 대표 부부가 「내 가게 얘기」 로 본다. | 가명 24명 — 시연 몰입이 떨어진다. 전화번호 실제값 — Demo 에 실제 개인정보 노출 금지. | 외부 공개 시연(매장 밖) 이 잡히면 → 화면 공유 모드(개인정보 가림) 로 시연. |
| D-08 | **고객 화면의 「예시(sample)」 와 `demoMode` 는 다른 플래그다** (`lib/portal/store.tsx`) | `demoMode` 는 빌드 시점 플래그이고 직원 로그인까지 푼다. 고객 화면 예시는 매장 연결 전에도 열려야 하므로 런타임 플래그가 따로 필요. | `demoMode` 하나로 겸용 — 실운영 빌드에서 고객 예시가 안 열리거나, 예시를 열려고 직원 게이트를 푸는 사고. | 없음. |
| D-09 | **첫 실행 환영 안내(WelcomeIntro)는 시연 빌드에서 뜨지 않는다** | 시연 10걸음이 첫 화면부터 시작되어야 한다. 환영 창이 먼저 뜨면 심사자 앞에서 한 번 더 닫아야 한다. | 시연에서도 띄우기. | 시연 시나리오가 「첫 실행」 자체를 보여 주기로 바뀌면. |
| D-10 | **자료에 없는 숫자는 만들지 않는다** — 예상 매출 · AI 확률 · 이용권 유효기간 · 고객 수 증가율 | v3.0 절대 금지 + 정책자금 심사에서 근거 없는 숫자는 감점이 아니라 실격. | 「예상 매출 +20%」 같은 슬라이드 숫자. | 실측이 생기면 그 값만 (출처와 함께). |
| D-11 | **새 설정 항목은 전부 선택(optional)이고 `??` 기본값을 둔다** (`palette?` · `privacyMode?` · `axOwner?` · `baseline?`) | 저장된 `settings` 는 통째로 덮어써지므로, 필수 항목을 더하면 옛 저장분이 깨진다. 추가만 하고 바꾸지 않는다. | 스키마 버전 올리고 마이그레이션 — 12명 명부에 과하다. | 필드가 20개를 넘어 기본값 관리가 흩어질 때. |
| D-12 | **증적은 CSV 한 장으로 내보낸다** (BASELINE · ACTION · RESULT · ADOPTION) | 심사자가 엑셀로 바로 연다. 유형 칸 하나로 「무엇을 했고, 뒤에 무엇이 일어났는가」 를 줄 세울 수 있다. | PDF 보고서 생성 — 꾸밀 여지가 생기고 검증이 어렵다. | 지점이 3곳 이상이 되어 지점별 묶음이 필요할 때. |
| D-13 | **프랜차이즈 이야기는 본점 → 직영점 → 가맹점 → 본사 통합이지, AX 를 파는 회사가 아니다** | 실제 사업 방향. 「SaaS 판매」 로 적으면 심사 유형이 달라진다. | 플랫폼 SaaS 서사. | 없음. |
| D-14 | **의료 표현은 쓰지 않는다** — 치료 · 진단 · 처방 · 효능 대신 케어 · 상담 · 프로그램 · 이용 | 법적 경계 + 브랜드. 화면 · 문서 · 코드 주석 전부. | — | 없음 (영구). |
| D-15 | **직원 화면의 연락 수단은 `tel:` 링크 하나다** (`components/ui/PhoneLink.tsx`) | 브리핑 권장 행동이 「연락」 인데 번호를 외워 옮겨 적는 것이 60대 손끝에서 실행을 막았다. 전화는 매장이 이미 하는 일이라 새 절차가 없다. | 문자 · 알림톡 · 대량 발송 — 절대 금지 범위이고 고객 동의 절차가 필요 (R-10). | 고객 동의 절차와 발송 업체 계약이 생기면 R-10 으로. |
| D-16 | **AX 코치의 Mission 은 사람이 「완료」를 눌러 끝낼 수 없다** (`lib/ax-coach/verify.ts`) | 눌러서 채우는 체크리스트는 아무것도 증명하지 못한다. 심사에서 "이건 눌러서 만든 숫자인가요" 한 마디에 무너진다. 그래서 CTA 는 업무 화면으로 보낼 뿐이고, 완료는 실제 방문 · 처리 · 요청 기록에서 찾는다. | 카드마다 「완료」 단추 — 만들기 쉽고 시연에서 잘 넘어가지만, 그 숫자는 증적이 아니다. | 없음 (영구). 이 규칙이 AX 코치의 존재 이유다. |
| D-17 | **방문 기록만 시각이 아니라 「개수」로 검증한다** | `visitedAt` 은 사람이 고르는 방문 날짜라 어제 방문을 오늘 적을 수 있다. 저장 시각이 없으므로 발행 시점 개수를 적어 두고 늘었는지를 본다. 과제 · 요청은 시스템이 찍은 시각이 있어 시각으로 본다. | `Visit` 에 `recordedAt` 추가 — 기존 저장분에 없는 값이라 마이그레이션이 필요하고, 이 규모에 과하다. | 방문 기록에 저장 시각을 남길 일이 생기면 (서버 이관 시 `created_at` 이 자연히 생긴다). |
| D-18 | **실증 준비도는 「목표치를 화면에 함께 적고」 그 비율로 낸다** (`COACH_TARGET`) | 목표 없이 "38%" 만 띄우면 그건 지어낸 숫자다. 「최근 4주 8건 / 목표 20건」 처럼 적으면 사람이 검산할 수 있다. 잴 수 없는 영역은 0% 가 아니라 「아직 측정 전」 — 0 은 "쟀는데 없다" 는 다른 뜻이다. | 업계 평균 대비 점수 — 이 매장 규모의 비교 자료가 없어 지어내야 한다. | 2호점 이상이 생겨 매장 간 비교가 실제로 가능해질 때. |
| D-19 | **CI 는 두 갈래 — 빠른 검사는 푸시마다, 브라우저 회귀는 PR · 수동 · 매일 새벽** (`.github/workflows/checks.yml`) | 타입이 깨진 채 다섯 커밋이 쌓이면 어디서 깨졌는지 찾는 데 더 오래 걸린다. 반면 브라우저 18묶음까지 푸시마다 돌리면 한 번 고칠 때마다 20분을 기다리게 되어, 사람이 CI 를 끄게 된다. | 전부 푸시마다 — 느려서 결국 무시된다. 전부 PR 에만 — 혼자 개발하는 지금은 PR 을 안 만든다. | 개발자가 늘어 PR 흐름이 자리 잡으면 빠른 쪽도 PR 로 옮겨도 된다. |
| D-20 | **증적 CSV 에 COACH 유형을 더하고, 충족되지 않은 것도 그대로 적는다** | ACTION 은 "처리했다" 를, COACH 는 "처리하자고 먼저 정해 두었고 그것이 실제 기록으로 충족됐다" 를 보인다. 심사의 「운영 체계가 있는가」 에 답하는 줄이다. 성공만 남기면 그 표는 증적이 아니라 홍보물이다. | 충족된 것만 내보내기 — 표가 예뻐지지만 검증이 불가능해진다. | 없음 (영구). |
| D-21 | **실증 리포트는 화면이 쓰는 함수를 그대로 써서 A4 한 장으로만 만든다** (`EvidenceSheet.tsx`) | 심사장에서 엑셀 700줄을 읽을 사람은 없다. 다만 종이는 맥락 없이 돌아다니므로 단계(DEMO/PILOT/PRODUCTION)를 종이 위에도 박는다 — 그 한 줄이 없으면 몇 달 뒤 누군가 시연 자료를 실적으로 읽는다. | PDF 생성기 — 꾸밀 여지가 생기고 화면 값과 어긋날 수 있다 (D-12 와 같은 이유). | 지점이 여럿이 되어 지점별 리포트가 필요할 때. |
| D-22 | **백업 알림은 「받을 것이 있고 · 7일 지났고 · 기기에만 저장되고 · 오늘 안 닫았을 때」 만 뜬다** | 리마인더가 설정 화면 안에만 있으면 설정을 여는 사람에게만 보인다. 그렇다고 매일 띄우면 사흘 만에 안 보이게 된다. 서버에 연결돼 있으면 기록이 두 곳에 있으므로 띄우지 않는다. | 항상 띄우기 / 설정 안에만 두기 — 둘 다 결과가 같다(아무도 백업하지 않는다). | 설정까지 서버 동기화되면(R-09) 조건을 다시 본다. |
| D-23 | **목차는 넷이다 — 오늘 · 고객 · 성과 · 더보기. 화면은 하나도 없애지 않는다** (`nav-items.ts` `NAV_SECTIONS`) | 열한 개를 세 묶음으로 나눠 두어도 왼쪽에 열한 줄이 선 것은 그대로였다. 항목이 많다는 건 쓰는 사람이 **매번 고른다**는 뜻이고, 그 고름을 하루에 수십 번 하면 그 자체가 일이 된다. 카카오톡은 넷이고, 그 넷 안에서 전부 일어난다. 주 사용자가 60대다. | 묶음에 접었다 펴는 손잡이 달기 — 여는 동작이 한 겹 더 늘고, 접힌 것은 없는 것이 된다. 화면을 실제로 줄이기 — 기능을 줄이지 말라는 것이 지시였다. | 화면이 늘어 한 묶음의 탭이 셋을 넘으면 → 탭을 줄이지 말고 묶음을 다시 나눈다. |
| D-24 | **목차에서 내려온 화면은 「묶음 안의 큰 탭」으로 간다** (`SectionTabs.tsx`) | 목차에서 뺀 화면이 두 번 눌러야 닿는 곳으로 가면 그건 정리가 아니라 숨김이다. 묶음에 들어서는 순간 형제 화면이 화면 맨 위에 통째로 펼쳐져 있으면, 누르는 횟수는 전과 같다. 높이 48px · 글자 17px · 지금 자리는 칠해진 바탕으로 — 색만으로 구분하는 곳은 없다. | 드롭다운 · 햄버거 · 「더보기」 안에 전부 넣기 — 셋 다 한 겹을 더 만든다. | 없음. 탭이 넷이 되려 하면 D-23 의 REVISIT 조건이 먼저 걸린다. |
| D-25 | **메뉴 아이콘 색은 두 갈래뿐이다 — 업무는 청록, 읽을 거리는 금색. 안에서는 진하기만 바꾼다** (`NAV_TONE_CLASS`) | 항목마다 색을 달리 줬더니(청록·하늘·보라·주황·초록·금색·회색) 구분이 아니라 소음이 됐다. 글자를 읽기 전에 눈이 먼저 지치고, 처음 보는 분에게는 「복잡한 프로그램」이라는 첫인상만 남는다. 글자색은 갈래마다 하나로 고정하고 바탕 투명도만 움직여서, 대비가 한 번 검증된 값 근처에 머물게 했다. | 항목마다 다른 색 — 어지럽다(대표님 지적). 전부 같은 색 같은 톤 — 그러면 그냥 같은 칸이라 순서가 안 읽힌다. | 없음 (영구). 색이 아니라 아이콘 모양과 글자가 구분한다. |
| D-26 | **폰에서는 긴 화면을 접는다 — 넓은 화면에서는 접지 않는다** (`SettingsSection` · `DocToc` · `DocSection`) | 설정이 360px 에서 **10.6 화면**이었다. 영업시간 하나 고치려고 열 번 쓸어내리면 원하던 칸을 두 번 지나치고 세 번째에 포기한다. 아이폰 · 안드로이드 설정이 오래 쓰는 방식이라 새로 배울 것도 없다. 반대로 PC 는 두 단 배치라 이미 다 보이고, 거기서 접으면 마우스 쓰는 사람에게 없던 일이 하나 생긴다. 접는 것은 **자리가 없을 때** 하는 일이지 언제나 좋은 일이 아니다. | 모든 폭에서 접기 — PC 에서 Ctrl+F 로 찾는 길이 막히고 투어(1440px)가 빈 카드를 비춘다. 화면을 여럿으로 쪼개기 — 주소가 늘고 바로가기가 깨진다. | 폰 한 칸이 세 화면을 넘어가면 그 칸을 다시 나눈다. |
| D-27 | **알림 띠는 폰에서 한 줄이다 — 안내가 그날 할 일을 가리지 않는다** (`BackupReminder`) | PC 와 같은 모양으로 그렸더니 폰에서 180px 였고, 이 띠는 직원 화면 **열두 장 전부**의 맨 위에 붙는다. 그래서 대시보드를 열면 「오늘 챙길 고객」 이, 고객 화면을 열면 첫 고객이 첫 장 밖에 있었다. 사진을 찍어 보고 알았다. 안내 하나가 그날 할 일을 가린다면 그건 안내가 아니다. | 띠를 없애기 — 백업은 실제로 밀리고 있고, 설정 안에만 두면 아무도 안 본다(D-22). 폰용 단추를 따로 만들기 — 같은 이름의 단추가 화면에 둘이 되어 사람도 검사기도 어느 쪽인지 모른다. | 없음. 새 띠를 더할 때도 같은 규칙(폰 100px 이내)을 지킨다. |
| D-28 | **AX 코치는 「오늘 할 일」 을 먼저, 준비도를 그 뒤에 놓는다** | 준비도를 위에 두었더니 폰에서 첫 Mission 이 1,482px — 화면 밖 둘째 장이었다. 이 화면을 아침에 여는 이유는 「오늘 뭐 하면 되나」 이지 「우리가 몇 퍼센트인가」 가 아니다. 점수는 그 일을 하고 나서 보는 것이다. | 준비도를 접거나 줄이기 — 목표치와 검산 숫자가 이 화면의 정직성 장치다(D-18). 자리만 바꿨다. | 없음. |
| D-29 | **폰 기준을 숫자로 못 박고 회귀가 지킨다** (`qa/mobile.mjs`) | 「폰에서도 되는지」 를 매번 사람이 사진 찍어 확인했고, 한 번 고쳐 놔도 다음 기능이 들어오면 조용히 되돌아갔다. 그래서 재는 것을 둘로 정했다 — **첫 일거리까지의 거리**(그 화면을 연 이유가 첫 장 안에 있는가)와 **화면 장수 상한**. 상한은 실측 + 여유 15% 라 내용이 조금 느는 것은 막지 않고, 다시 열 화면짜리가 되는 것만 막는다. | 「폰에서 예쁜지」 를 재기 — 사람마다 답이 다르고 자동으로 못 센다. 상한 없이 사진만 남기기 — 지금까지 그래서 되돌아갔다. | 화면이 늘어 상한을 올려야 하면, 올리기 전에 왜 길어졌는지를 먼저 본다. |
